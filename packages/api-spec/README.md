# @ticqex/api-spec

Shared operation catalog for the Ticqex REST API (`/api/v1/*`).

Used by:

- `@ticqex/cli` — command routing
- `pnpm openapi:generate` — OpenAPI spec generation

Each operation maps an MCP tool name to an HTTP method and path template.
